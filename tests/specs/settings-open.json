[
  {
    "name": "Settings",
    "steps": [
      {
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Open settings for quiet hours, morning summary time, cooldown length, and timezone"
            }
          }
        ],
        "send": {
          "callback": "settings:open"
        }
      }
    ]
  }
]
