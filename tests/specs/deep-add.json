[
  {
    "name": "Add via deep link (optional)",
    "steps": [
      {
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Start a pre-filled /start flow to add a ticker passed in the deep-link"
            }
          }
        ],
        "send": {
          "callback": "deep:add"
        }
      }
    ]
  }
]
